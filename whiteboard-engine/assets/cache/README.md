# Local Cache

Downloaded SVGs, source/license sidecars, narration WAVs, and temporary web jobs
are cached here. This directory contains private script-derived data.

It is safe to delete generated cache files while no render is running. Deleting
the cache removes offline access to downloaded icons and synthesized narration.
Bundled illustrations and backup SVGs remain available.